import { connect } from 'react-redux'
import RequestVisit from './RequestVisit'

const mapDispatchToProps = {
}

const mapStateToProps = (state) => ({
})

export default connect(mapStateToProps, mapDispatchToProps)(RequestVisit)
